---
phase: 18-agent-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/agents/skills/system_brief.md
  - backend/app/agents/skills/canvas_agent.md
  - backend/app/agents/skills/build_agent.md
  - backend/app/agents/skills/cube_expert.md
  - backend/app/agents/skills/validation_agent.md
  - backend/app/agents/skills/results_interpreter.md
  - backend/app/agents/skills_loader.py
autonomous: true
requirements: [INFRA-03, INFRA-04]

must_haves:
  truths:
    - "All five agent persona skill files exist and are non-empty"
    - "System brief contains Tracer 42 domain context"
    - "get_system_prompt combines system_brief + persona into one string"
    - "Skill files are loaded once at startup and cached in memory"
  artifacts:
    - path: "backend/app/agents/skills/system_brief.md"
      provides: "Tracer 42 domain context for all agents"
      min_lines: 20
    - path: "backend/app/agents/skills/canvas_agent.md"
      provides: "Canvas Agent persona prompt"
      min_lines: 5
    - path: "backend/app/agents/skills/build_agent.md"
      provides: "Build Wizard Agent persona prompt"
      min_lines: 5
    - path: "backend/app/agents/skills/cube_expert.md"
      provides: "Cube Expert persona prompt"
      min_lines: 5
    - path: "backend/app/agents/skills/validation_agent.md"
      provides: "Validation Agent persona prompt"
      min_lines: 5
    - path: "backend/app/agents/skills/results_interpreter.md"
      provides: "Results Interpreter persona prompt"
      min_lines: 5
    - path: "backend/app/agents/skills_loader.py"
      provides: "Skill loading and caching module"
      exports: ["load_skill_files", "get_skill", "get_system_prompt"]
  key_links:
    - from: "backend/app/agents/skills_loader.py"
      to: "backend/app/agents/skills/"
      via: "Path.glob('*.md') at startup"
      pattern: "SKILLS_DIR.*glob.*\\.md"
---

<objective>
Create agent skill files (system prompts) and the loader module that caches them at startup.

Purpose: Every agent persona needs a system prompt loaded from disk. The system brief provides Tracer 42 domain context injected at the top of every agent call. This plan creates all skill files and the loader, independent of the Gemini client (Plan 01) and tool registry (Plan 03).

Output: Six markdown skill files in `backend/app/agents/skills/`, plus `skills_loader.py` with `load_skill_files()`, `get_skill()`, `get_system_prompt()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-agent-infrastructure/18-CONTEXT.md
@.planning/phases/18-agent-infrastructure/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill loader module</name>
  <files>backend/app/agents/skills_loader.py</files>
  <read_first>
    - backend/app/agents/__init__.py
  </read_first>
  <action>
Create `backend/app/agents/skills_loader.py` with the following implementation (per D-01, D-02, D-03, research Pattern 6):

```python
"""Skill file loader — loads agent persona prompts from disk at startup."""

import logging
from pathlib import Path

log = logging.getLogger(__name__)

_skills: dict[str, str] = {}
SKILLS_DIR = Path(__file__).parent / "skills"


def load_skill_files() -> None:
    """Load all .md skill files from the skills directory into memory.

    Called once during FastAPI lifespan startup. Requires server restart
    to pick up prompt changes (uvicorn --reload handles dev).
    """
    for md_file in SKILLS_DIR.glob("*.md"):
        _skills[md_file.stem] = md_file.read_text(encoding="utf-8")
        log.info("Loaded skill file: %s", md_file.stem)
    log.info("Loaded %d skill files total", len(_skills))


def get_skill(name: str) -> str:
    """Return a skill's content by name (filename stem), or empty string if not found."""
    return _skills.get(name, "")


def get_system_prompt(persona: str) -> str:
    """Combine system_brief + persona skill into one system prompt string.

    The system brief (Tracer 42 domain context) is always prepended.
    Returns just the persona text if system_brief is missing.
    """
    brief = _skills.get("system_brief", "")
    persona_text = _skills.get(persona, "")
    return f"{brief}\n\n{persona_text}".strip()


def get_all_personas() -> list[str]:
    """Return names of all loaded skills except system_brief."""
    return [name for name in _skills if name != "system_brief"]
```

Key requirements:
- `SKILLS_DIR` is `Path(__file__).parent / "skills"` (relative to skills_loader.py)
- `load_skill_files()` uses `.glob("*.md")` to discover all markdown files
- `get_system_prompt()` concatenates `system_brief` + persona with `\n\n` separator
- Logging via `logging.getLogger(__name__)` (project pattern)
- `_skills` is module-level dict (cached in memory per D-03)
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/12-flow/backend && uv run python -c "from app.agents.skills_loader import load_skill_files, get_skill, get_system_prompt; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/agents/skills_loader.py contains `def load_skill_files() -> None:`
    - backend/app/agents/skills_loader.py contains `def get_skill(name: str) -> str:`
    - backend/app/agents/skills_loader.py contains `def get_system_prompt(persona: str) -> str:`
    - backend/app/agents/skills_loader.py contains `SKILLS_DIR = Path(__file__).parent / "skills"`
    - backend/app/agents/skills_loader.py contains `_skills: dict[str, str] = {}`
    - backend/app/agents/skills_loader.py contains `system_brief`
  </acceptance_criteria>
  <done>Skills loader module importable with all three public functions.</done>
</task>

<task type="auto">
  <name>Task 2: Create system brief and five agent persona skill files</name>
  <files>
    backend/app/agents/skills/system_brief.md,
    backend/app/agents/skills/canvas_agent.md,
    backend/app/agents/skills/build_agent.md,
    backend/app/agents/skills/cube_expert.md,
    backend/app/agents/skills/validation_agent.md,
    backend/app/agents/skills/results_interpreter.md
  </files>
  <read_first>
    - backend/app/agents/skills_loader.py
    - backend/app/schemas/cube.py
    - backend/app/schemas/workflow.py
  </read_first>
  <action>
Create the `backend/app/agents/skills/` directory and six markdown files (per D-01, D-02).

**1. `system_brief.md`** — Tracer 42 domain context injected at top of every agent prompt. Must contain:
- Project name: "Tracer 42 — Visual Dataflow Workflow Builder for Flight Analysis"
- What it does: Users build analysis pipelines by dragging cubes onto a canvas, configuring parameters, connecting outputs to inputs, and executing against a PostgreSQL database containing flight tracking data (ADS-B transponder data from OpenSky Network and Alison systems)
- Core concepts: Cubes (processing nodes with typed input/output parameters), Connections (parameter-level wiring between cubes), Execution (topological sort, results capped at 100k rows)
- Cube categories: data_source (fetch flight data), filter (narrow results), analysis (detect patterns), aggregation (summarize), output (format results)
- Available data sources: `all_flights` (OpenSky Network), `alison_flights` (Alison system), `get_anomalies`, `get_flight_course`, `get_learned_paths`
- Key filters: `squawk_filter` (emergency/special codes), `registration_country_filter` (by ICAO24 prefix), `area_spatial_filter` (geographic polygon filtering)
- Analysis cubes: `signal_health_analyzer` (ADS-B signal integrity detection), `geo_temporal_playback`
- Utility cubes: `echo`, `add_numbers`, `count_by_field`
- Important: Database is read-only. Results are capped. Users are flight analysts, not developers.

**2. `canvas_agent.md`** — Canvas Agent persona. Placeholder content for Phase 20:
```
# Canvas Agent

You are the Canvas Agent for Tracer 42. You help analysts modify, optimize, and fix their workflow graphs on the canvas.

## Capabilities
- Analyze the current workflow graph and suggest improvements
- Diagnose errors from the last workflow execution
- Add, remove, or reconfigure cubes and connections
- Explain what a workflow does in plain language

## Modes
- **Optimize**: Suggest faster or simpler cube configurations
- **Fix**: Read error output and diagnose pipeline failures
- **General**: Answer questions, find cubes, suggest edits

## Rules
- Always call get_cube_definition before suggesting parameter values
- Never suggest cubes that don't exist in the catalog
- Explain your reasoning before proposing changes
- Propose changes as a complete diff, not incremental edits
```

**3. `build_agent.md`** — Build Wizard Agent persona. Placeholder for Phase 21:
```
# Build Wizard Agent

You are the Build Wizard for Tracer 42. You guide analysts through creating a new workflow from scratch using structured questions and option cards.

## Process
1. Discover the analyst's mission (what they want to analyze)
2. Recommend a data source based on their needs
3. Suggest relevant filters and analysis cubes
4. Generate a complete workflow graph

## Rules
- Present options as structured choices, not free text
- Always validate the generated workflow before delivering it
- Save the mission intent to workflow metadata for later reference
- Never hallucinate cube names — always verify against the catalog
```

**4. `cube_expert.md`** — Cube Expert sub-agent persona. Placeholder for Phase 19:
```
# Cube Expert

You are the Cube Expert for Tracer 42. You are a sub-agent called by other agents to answer questions about available cubes, their parameters, and how to connect them.

## Capabilities
- Browse the cube catalog by category
- Look up detailed parameter definitions for any cube
- Recommend which cube fits a described use case
- Explain parameter types and connection compatibility

## Rules
- Always call browse_catalog first to see what's available
- Then call get_cube_definition for specific cubes before answering parameter questions
- Never guess parameter handles — always look them up
- Keep responses concise — you are called by other agents, not directly by users
```

**5. `validation_agent.md`** — Validation Agent persona. Placeholder for Phase 19:
```
# Validation Agent

You are the Validation Agent for Tracer 42. You check workflow graphs for structural issues before execution.

## Checks
- Missing required parameters on cubes
- Dangling inputs (connected to non-existent outputs)
- Type mismatches between connected parameters
- Cycles in the workflow graph

## Rules
- Report issues with specific cube names and parameter names
- Explain why each issue matters (what would fail at execution)
- Suggest how to fix each issue
- Return a clean result when no issues are found
```

**6. `results_interpreter.md`** — Results Interpreter persona. Placeholder for Phase 22:
```
# Results Interpreter

You are the Results Interpreter for Tracer 42. You analyze workflow execution results and explain them in the context of the analyst's mission.

## Capabilities
- Summarize execution results with flight-analysis framing
- Reference the analyst's original mission intent when available
- Highlight notable patterns (anomaly counts, geographic clusters, temporal trends)

## Rules
- Never dump raw data — summarize and interpret
- If mission context exists, frame the interpretation around it
- If no mission context exists, use generic flight-analysis framing
- Keep interpretations concise (3-5 key findings)
```

All persona files are initial placeholders — their content will be refined when each agent is implemented in Phases 19-22. The important thing is that the files exist so the loader works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/12-flow/backend && uv run python -c "
from app.agents.skills_loader import load_skill_files, get_skill, get_system_prompt, get_all_personas
load_skill_files()
# Verify all 6 files loaded
personas = get_all_personas()
assert len(personas) == 5, f'Expected 5 personas, got {len(personas)}: {personas}'
# Verify system brief exists and is non-empty
brief = get_skill('system_brief')
assert len(brief) > 100, f'system_brief too short: {len(brief)} chars'
# Verify get_system_prompt combines correctly
prompt = get_system_prompt('canvas_agent')
assert 'Tracer 42' in prompt, 'system_brief not in combined prompt'
assert 'Canvas Agent' in prompt, 'persona not in combined prompt'
print(f'OK: {len(personas)} personas, brief={len(brief)} chars')
"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/agents/skills/system_brief.md exists and contains "Tracer 42"
    - backend/app/agents/skills/canvas_agent.md exists and contains "Canvas Agent"
    - backend/app/agents/skills/build_agent.md exists and contains "Build Wizard"
    - backend/app/agents/skills/cube_expert.md exists and contains "Cube Expert"
    - backend/app/agents/skills/validation_agent.md exists and contains "Validation Agent"
    - backend/app/agents/skills/results_interpreter.md exists and contains "Results Interpreter"
    - `load_skill_files()` loads all 6 files (5 personas + 1 system_brief)
    - `get_system_prompt('canvas_agent')` returns string containing both "Tracer 42" and "Canvas Agent"
  </acceptance_criteria>
  <done>All six skill files exist. Loader discovers and caches them. get_system_prompt correctly combines system_brief with any persona.</done>
</task>

</tasks>

<verification>
- All 6 .md files exist in `backend/app/agents/skills/`
- `load_skill_files()` loads all files without error
- `get_system_prompt('canvas_agent')` contains both domain context and persona
- `get_all_personas()` returns exactly 5 persona names (excluding system_brief)
</verification>

<success_criteria>
- Skills directory contains system_brief.md + 5 persona files
- system_brief.md contains substantial Tracer 42 domain context (cube categories, data sources, analyst context)
- skills_loader.py loads all files and provides get_system_prompt combining brief + persona
- No hardcoded skill content in Python code — all loaded from .md files on disk
</success_criteria>

<output>
After completion, create `.planning/phases/18-agent-infrastructure/18-02-SUMMARY.md`
</output>
