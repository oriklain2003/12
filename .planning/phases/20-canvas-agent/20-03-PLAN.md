---
phase: 20-canvas-agent
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - frontend/src/components/Chat/ChatPanel.tsx
  - frontend/src/components/Chat/ChatPanel.css
  - frontend/src/components/Chat/ModeToggle.tsx
  - frontend/src/components/Chat/MessageList.tsx
  - frontend/src/components/Chat/MessageBubble.tsx
  - frontend/src/components/Chat/DiffProposal.tsx
  - frontend/src/components/Chat/ChatInput.tsx
  - frontend/src/components/Chat/ToolCallIndicator.tsx
autonomous: true
requirements: [CANVAS-01, CANVAS-02, CANVAS-04, CANVAS-06]
must_haves:
  truths:
    - "Chat panel renders as a right sidebar in the editor layout"
    - "Three-segment mode toggle switches between Optimize, Fix, and General"
    - "Mode switch does NOT clear chat messages (per D-07)"
    - "User messages appear right-aligned, agent messages appear left-aligned with accent border"
    - "Streaming agent messages show a blinking cursor"
    - "DiffProposal block shows structured add/remove/update items with Apply Changes and Reject Changes buttons"
    - "Apply Changes calls applyAgentDiff from the store"
    - "Chat input sends message via streamAgentChat and renders streaming response"
    - "Session ID is captured from first SSE event and reused across turns"
    - "Panel is resizable via drag handle on left edge"
  artifacts:
    - path: "frontend/src/components/Chat/ChatPanel.tsx"
      provides: "Right sidebar shell with drag handle, header, and child components"
      min_lines: 80
    - path: "frontend/src/components/Chat/ChatPanel.css"
      provides: "Panel layout, drag handle, left-edge gradient, glass styling"
      min_lines: 100
    - path: "frontend/src/components/Chat/ModeToggle.tsx"
      provides: "Three-segment toggle control"
      contains: "optimize"
    - path: "frontend/src/components/Chat/MessageList.tsx"
      provides: "Scrollable message thread with auto-scroll"
      contains: "MessageBubble"
    - path: "frontend/src/components/Chat/MessageBubble.tsx"
      provides: "Individual message rendering with streaming support"
      contains: "streaming"
    - path: "frontend/src/components/Chat/DiffProposal.tsx"
      provides: "Diff summary with Apply/Reject buttons"
      contains: "Apply Changes"
    - path: "frontend/src/components/Chat/ChatInput.tsx"
      provides: "Textarea with send button"
      contains: "Send"
    - path: "frontend/src/components/Chat/ToolCallIndicator.tsx"
      provides: "Inline spinner during tool dispatch"
      contains: "spinner"
  key_links:
    - from: "frontend/src/components/Chat/ChatPanel.tsx"
      to: "frontend/src/store/flowStore.ts"
      via: "useFlowStore selectors for chatPanelOpen, chatPanelMode"
      pattern: "useFlowStore"
    - from: "frontend/src/components/Chat/ChatInput.tsx"
      to: "frontend/src/api/agent.ts"
      via: "streamAgentChat call on send"
      pattern: "streamAgentChat"
    - from: "frontend/src/components/Chat/DiffProposal.tsx"
      to: "frontend/src/store/flowStore.ts"
      via: "applyAgentDiff on Apply click"
      pattern: "applyAgentDiff"
---

<objective>
Build all eight ChatPanel UI components: the sidebar shell with drag-resize, mode toggle, message list, message bubble (with streaming), diff proposal (with Apply/Reject), chat input, and tool call indicator. These components consume the store state and API client from Plan 02.

Purpose: This is the visible chat interface analysts interact with. It renders the agent conversation, handles SSE streaming into message bubbles, and presents diff proposals for review.

Output: Eight React components in `frontend/src/components/Chat/` with matching CSS, fully wired to the Zustand store and SSE streaming client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-canvas-agent/20-CONTEXT.md
@.planning/phases/20-canvas-agent/20-RESEARCH.md
@.planning/phases/20-canvas-agent/20-UI-SPEC.md
@.planning/phases/20-canvas-agent/20-02-SUMMARY.md

<interfaces>
<!-- Types and store contracts from Plan 02 -->

From frontend/src/types/agent.ts (created by Plan 02):
```typescript
export type AgentMode = 'optimize' | 'fix' | 'general';
export interface ChatMessage {
  id: string; role: 'user' | 'agent'; content: string; timestamp: number;
  type?: string; toolName?: string; diff?: AgentDiff; streaming?: boolean;
}
export interface AgentDiff {
  summary?: string;
  add_nodes?: Array<{ id?: string; cube_id: string; position: { x: number; y: number }; params?: Record<string, unknown>; label?: string }>;
  remove_node_ids?: string[];
  update_params?: Array<{ node_id: string; params: Record<string, unknown> }>;
  add_edges?: Array<{ id?: string; source: string; target: string; source_handle?: string; target_handle?: string }>;
  remove_edge_ids?: string[];
}
export interface AgentSSEEvent {
  type: 'text' | 'tool_call' | 'tool_result' | 'thinking' | 'done' | 'session';
  data: string | Record<string, unknown> | null;
}
```

From frontend/src/store/flowStore.ts (added by Plan 02):
```typescript
// Chat panel state
chatPanelOpen: boolean;
chatPanelMode: AgentMode;
chatMessages: ChatMessage[];
chatSessionId: string | null;
pendingDiff: AgentDiff | null;
isAgentStreaming: boolean;

// Chat actions
setChatPanelOpen: (open: boolean) => void;
setChatPanelMode: (mode: AgentMode) => void;
addChatMessage: (msg: ChatMessage) => void;
updateLastAgentMessage: (content: string) => void;
setChatSessionId: (id: string | null) => void;
setPendingDiff: (diff: AgentDiff | null) => void;
setIsAgentStreaming: (streaming: boolean) => void;
clearChat: () => void;
applyAgentDiff: (diff: AgentDiff) => void;

// Existing state used by chat:
nodes: CubeFlowNode[];
edges: Edge[];
workflowId: string | null;
results: Record<string, { rows: unknown[]; truncated: boolean }>;
executionStatus: Record<string, { status: CubeStatus; error?: string; ... }>;
```

From frontend/src/api/agent.ts (added by Plan 02):
```typescript
export async function* streamAgentChat(
  message: string, sessionId: string | null, workflowId: string | null,
  workflowGraph: WorkflowGraph | null, mode: AgentMode,
  executionErrors?: Record<string, unknown> | null,
  executionResults?: Record<string, unknown> | null,
): AsyncGenerator<AgentSSEEvent>;
```

From frontend/src/store/flowStore.ts (existing):
```typescript
export function serializeGraph(nodes: CubeFlowNode[], edges: Edge[]): WorkflowGraph;
```

CSS variables from theme.css (all already defined):
- --color-bg, --color-surface, --color-surface-raised
- --color-border, --color-border-focus
- --color-text, --color-text-secondary, --color-text-dim, --color-text-muted, --color-text-placeholder
- --color-accent, --color-accent-glow, --accent-rgb
- --color-error, --color-warning, --color-success
- --radius-sm, --radius-md
- --btn-accent-bg, --btn-accent-border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatPanel shell, ChatPanel.css, ModeToggle, and ToolCallIndicator</name>
  <files>frontend/src/components/Chat/ChatPanel.tsx, frontend/src/components/Chat/ChatPanel.css, frontend/src/components/Chat/ModeToggle.tsx, frontend/src/components/Chat/ToolCallIndicator.tsx</files>
  <read_first>
    frontend/src/components/Sidebar/CubeCatalog.tsx
    frontend/src/components/Sidebar/CubeCatalog.css
    frontend/src/styles/theme.css
    frontend/src/styles/glass.css
    frontend/src/components/Validation/IssuesPanel.tsx
    .planning/phases/20-canvas-agent/20-UI-SPEC.md
  </read_first>
  <action>
    Build four components following the UI-SPEC exactly. Reference existing CubeCatalog for sidebar patterns and IssuesPanel for panel header patterns.

    **ChatPanel.tsx** — Right sidebar shell:
    - Reads `chatPanelOpen`, `chatPanelMode` from flowStore via `useFlowStore`
    - If `!chatPanelOpen`, render nothing (per UI-SPEC: `display: none`, not icon strip)
    - Renders drag handle div on left edge with `mousedown`/`mousemove`/`mouseup` resize logic
      - Store panel width in local `useState`, default 320px, min 240px, max 480px
      - On mousedown on handle: attach mousemove/mouseup listeners to document
      - On mousemove: calculate new width as `window.innerWidth - e.clientX - (width of left sidebar if open)`; simpler: just `document.body.clientWidth - e.clientX` then clamp
      - On mouseup: remove listeners
    - Panel left edge animated gradient line: CSS `::after` pseudo-element matching `.sidebar::after` pattern from CubeCatalog.css but on `left: 0` instead of `right: 0`
    - Panel header: 44px height, `var(--color-surface-raised)` bg, border-bottom
      - "AGENT" title: 11px, weight 600, uppercase, letter-spacing 0.06em, `var(--color-text-muted)`
      - `<ModeToggle />` component
      - Close button: 28x28px, same style as `.sidebar__toggle`, `aria-label="Close agent panel"`, calls `setChatPanelOpen(false)`
    - Panel body: contains `<MessageList />`, `<ChatInput />`
    - Full component class name: `chat-panel`
    - Background: `linear-gradient(180deg, var(--color-surface) 0%, #050609 100%)` — mirrors CubeCatalog
    - `flex-shrink: 0`, `height: 100%`

    **ChatPanel.css** — Complete styling per UI-SPEC:
    - `.chat-panel` — flex column, height 100%, background gradient, position relative
    - `.chat-panel__drag-handle` — 8px wide, full height, left edge, cursor col-resize, transparent normally, `rgba(255,255,255,0.04)` on hover
    - `.chat-panel::after` — animated gradient line on left edge (copy `.sidebar::after` pattern, change `right` to `left`)
    - `.chat-panel__header` — 44px height, surface-raised bg, border-bottom, display flex, align-items center, padding 0 12px, gap 8px
    - `.chat-panel__title` — 11px, weight 600, uppercase, letter-spacing 0.06em, color text-muted
    - `.chat-panel__close-btn` — 28x28px, border-radius 50%, matches `.sidebar__toggle` style
    - `.chat-panel__body` — flex 1, display flex, flex-direction column, overflow hidden
    - Message bubble styles (user and agent) per UI-SPEC Message Bubble Styling section
    - `.chat-panel__empty` — centered empty state text

    **ModeToggle.tsx** — Three-segment toggle per UI-SPEC:
    - Container: flex, border-radius var(--radius-sm), background rgba(255,255,255,0.04), border 1px solid var(--color-border), padding 4px
    - Three segments: 'optimize', 'fix', 'general'
    - Labels: "Optimize", "Fix", "General"
    - Each segment: padding 0 12px, height 24px, border-radius 6px, font-size 11px, font-weight 600, cursor pointer
    - Inactive: color var(--color-text-dim), transparent bg
    - Active: color var(--color-text), background rgba(var(--accent-rgb), 0.18), border 1px solid rgba(var(--accent-rgb), 0.25)
    - Fix segment when active AND errors exist (check `executionStatus` from store for any status === 'error'): show 4px colored dot using var(--color-error) at left of "Fix" label
    - On click: calls `setChatPanelMode(mode)` — does NOT clear chat messages (per D-07)
    - Props: none needed — reads from store directly

    **ToolCallIndicator.tsx** — Inline row during tool dispatch:
    - Props: `toolName: string`
    - Display flex, align-items center, gap 8px, padding 8px 12px
    - Spinner: 12x12px SVG circle with stroke-dasharray animation, var(--color-text-muted)
    - Label: 11px, weight 400, var(--color-text-muted)
    - Label text mapped from tool name per UI-SPEC copywriting:
      - `read_workflow_graph` -> "Reading workflow graph..."
      - `read_execution_errors` -> "Reading execution errors..."
      - `read_execution_results` -> "Reading execution results (capped at store limit)..."
      - `get_cube_definition` -> "Looking up cube definition..."
      - `list_cubes_summary` -> "Browsing cube catalog..."
      - `find_cubes_for_task` -> "Browsing cube catalog..."
      - default -> `"Running ${toolName}..."`
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/12-flow/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/Chat/ChatPanel.tsx` contains `useFlowStore`
    - `frontend/src/components/Chat/ChatPanel.tsx` contains `chatPanelOpen`
    - `frontend/src/components/Chat/ChatPanel.tsx` contains `aria-label="Close agent panel"`
    - `frontend/src/components/Chat/ChatPanel.tsx` contains `chat-panel__drag-handle`
    - `frontend/src/components/Chat/ChatPanel.tsx` contains `ModeToggle`
    - `frontend/src/components/Chat/ChatPanel.tsx` contains `MessageList`
    - `frontend/src/components/Chat/ChatPanel.tsx` contains `ChatInput`
    - `frontend/src/components/Chat/ChatPanel.css` contains `chat-panel__header`
    - `frontend/src/components/Chat/ChatPanel.css` contains `linear-gradient(180deg`
    - `frontend/src/components/Chat/ChatPanel.css` contains `col-resize`
    - `frontend/src/components/Chat/ModeToggle.tsx` contains `setChatPanelMode`
    - `frontend/src/components/Chat/ModeToggle.tsx` does NOT contain `clearChat` or clearing messages
    - `frontend/src/components/Chat/ModeToggle.tsx` contains `Optimize`
    - `frontend/src/components/Chat/ToolCallIndicator.tsx` contains `Reading workflow graph`
    - `frontend/src/components/Chat/ToolCallIndicator.tsx` contains `stroke-dasharray`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>ChatPanel shell renders as resizable right sidebar with header, mode toggle, close button, drag handle, and slots for message list and input. ToolCallIndicator shows inline spinner with tool-specific labels.</done>
</task>

<task type="auto">
  <name>Task 2: Create MessageList, MessageBubble, DiffProposal, and ChatInput with SSE streaming</name>
  <files>frontend/src/components/Chat/MessageList.tsx, frontend/src/components/Chat/MessageBubble.tsx, frontend/src/components/Chat/DiffProposal.tsx, frontend/src/components/Chat/ChatInput.tsx</files>
  <read_first>
    frontend/src/components/Chat/ChatPanel.tsx
    frontend/src/components/Chat/ChatPanel.css
    frontend/src/store/flowStore.ts
    frontend/src/api/agent.ts
    .planning/phases/20-canvas-agent/20-UI-SPEC.md
  </read_first>
  <action>
    Build the four remaining chat UI components. These handle message rendering, diff proposal interaction, and the send flow with SSE streaming.

    **MessageList.tsx** — Scrollable message thread:
    - Reads `chatMessages` from flowStore
    - Container: `flex: 1`, `overflow-y: auto`, `padding: 12px 0`
    - Auto-scroll to bottom on new messages: `useEffect` with `useRef` on container, `scrollTop = scrollHeight` when `chatMessages.length` changes
    - For each message, render:
      - If `message.type === 'tool_call'`: render `<ToolCallIndicator toolName={message.toolName ?? ''} />`
      - If `message.diff`: render `<MessageBubble />` followed by `<DiffProposal diff={message.diff} />`
      - Otherwise: render `<MessageBubble message={message} />`
    - Empty state (no messages): centered text per UI-SPEC copywriting:
      - Heading: "No messages yet" — 13px, weight 600, color var(--color-text-dim)
      - Body text varies by mode from store:
        - general: "Ask me about your workflow, find cubes, or suggest improvements."
        - optimize: "Describe a performance or simplification goal and I'll suggest changes to the canvas."
        - fix: "Run your workflow first, then I can read the errors and help diagnose."
      - Body: 12px, weight 400, color var(--color-text-muted), max-width 240px, text-align center

    **MessageBubble.tsx** — Single message with streaming cursor:
    - Props: `message: ChatMessage`
    - User message styling per UI-SPEC:
      - `background: rgba(255,255,255,0.05)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-sm)`
      - `padding: 8px 12px`, `margin: 0 12px 8px 24px` (right-aligned weight)
      - Text: 13px, weight 400, var(--color-text), line-height 1.5
    - Agent message styling per UI-SPEC:
      - `background: transparent`, `border-left: 2px solid rgba(var(--accent-rgb), 0.3)`
      - `padding: 8px 12px 8px 16px`, `margin: 0 24px 8px 12px` (left-aligned)
      - Text: 13px, weight 400, var(--color-text-secondary), line-height 1.5
    - Streaming state: if `message.streaming === true`, add a `::after` pseudo-element — 2px wide blinking vertical bar, var(--color-accent). CSS class: `message-bubble--streaming`. CSS animation:
      ```css
      @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
      .message-bubble--streaming::after {
        content: ''; display: inline-block; width: 2px; height: 1em;
        background: var(--color-accent); margin-left: 2px; vertical-align: text-bottom;
        animation: blink 1s step-end infinite;
      }
      ```
    - Content rendering: use `dangerouslySetInnerHTML` is NOT needed — just render `message.content` as text. If more formatting needed later, can add markdown. For now, plain text with `white-space: pre-wrap`.

    **DiffProposal.tsx** — Structured diff with Apply/Reject:
    - Props: `diff: AgentDiff`
    - Container per UI-SPEC Diff Proposal Block section:
      - `background: var(--color-surface-raised)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-md)` (14px)
      - `margin: 8px 12px`, `overflow: hidden`
    - Diff header: 36px height, `rgba(255,255,255,0.02)` bg, border-bottom, label "Proposed Changes" (11px, weight 600, uppercase, letter-spacing 0.06em, var(--color-text-muted))
    - Diff line items: iterate over diff fields and render:
      - `diff.add_nodes` -> each: `+` badge (green) + `"Add {cube_id} cube"` (cube_id in monospace)
      - `diff.remove_node_ids` -> each: `-` badge (red) + `"Remove node {id}"`
      - `diff.update_params` -> each: `~` badge (orange) + `"Update params on node {node_id}"` + list param names being changed
      - `diff.add_edges` -> each: `+` badge (green) + `"Connect {source} -> {target}"`
      - `diff.remove_edge_ids` -> each: `-` badge (red) + `"Remove connection {id}"`
    - Badge styling per UI-SPEC: 3px border-radius, padding 0 4px, font-weight 600
      - Add: `var(--color-success)` text, `rgba(34,197,94,0.1)` bg
      - Remove: `var(--color-error)` text, `rgba(239,68,68,0.1)` bg
      - Update: `var(--color-warning)` text, `rgba(249,115,22,0.1)` bg
    - Footer: padding 8px 12px, display flex, gap 8px, justify-content flex-end
      - "Apply Changes" button: class `glass-btn glass-btn--accent`, font-size 13px, font-weight 600, padding 8px 16px
        - On click: read `pendingDiff` from store (or use the `diff` prop), call `useFlowStore.getState().applyAgentDiff(diff)`, show `toast.success('Changes applied to canvas')` per UI-SPEC copywriting
      - "Reject Changes" button: class `glass-btn`, font-size 13px, font-weight 600, padding 8px 16px
        - On click: call `useFlowStore.getState().setPendingDiff(null)`, remove the diff from the message (update the chatMessages array to clear the diff field on this message)
    - Track local `applied` state: after Apply, show "Applied" label and hide both buttons. After Reject, remove the entire DiffProposal block.

    **ChatInput.tsx** — Textarea + Send button:
    - Local state: `input` string, managed by `useState`
    - Reads `isAgentStreaming`, `chatPanelMode`, `chatSessionId`, `workflowId`, `nodes`, `edges`, `executionStatus`, `results` from store
    - Textarea per UI-SPEC Chat Input Row:
      - `background: rgba(255,255,255,0.03)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-sm)`
      - `color: var(--color-text)`, `font-size: 13px`, `font-family: inherit`, `line-height: 1.5`
      - `padding: 8px 12px`, `min-height: 36px`, `max-height: 120px`, `resize: none`
      - Auto-grow: on input, set `textarea.style.height = 'auto'` then `textarea.style.height = textarea.scrollHeight + 'px'`
      - Placeholder varies by mode:
        - general: "Ask about your workflow..."
        - optimize: "Describe what you want to optimize..."
        - fix: "Describe the issue or let me diagnose..."
      - Focus ring: `border-color: var(--color-border-focus)`, `box-shadow: 0 0 0 3px var(--color-accent-glow)`
      - Enter sends message; Shift+Enter inserts newline (onKeyDown handler)
    - Send button per UI-SPEC:
      - 32x32px, `border-radius: var(--radius-sm)`, class `glass-btn glass-btn--accent`
      - Arrow-up SVG icon 12x12, inline, stroke-based, stroke-width 1.5
      - `aria-label="Send message"`, `title="Send (Enter)"`
      - Disabled when input is empty or `isAgentStreaming` is true: `opacity: 0.4`, `cursor: not-allowed`
    - **Send flow** (the core SSE integration):
      1. On send (Enter or button click):
      2. If input is empty or isAgentStreaming, return
      3. Add user message to store: `addChatMessage({ id: crypto.randomUUID(), role: 'user', content: input, timestamp: Date.now() })`
      4. Clear input field
      5. Add placeholder agent message: `addChatMessage({ id: crypto.randomUUID(), role: 'agent', content: '', timestamp: Date.now(), streaming: true })`
      6. Set `setIsAgentStreaming(true)`
      7. Serialize current graph: `const graph = serializeGraph(nodes, edges)`
      8. Build execution errors from executionStatus: `const errors = Object.fromEntries(Object.entries(executionStatus).filter(([_, s]) => s.status === 'error').map(([id, s]) => [id, { status: s.status, error: s.error }]))`
      9. Call `streamAgentChat(input, chatSessionId, workflowId, graph, chatPanelMode, errors, results)`
      10. For each SSE event yielded:
          - `type === 'session'`: call `setChatSessionId((event.data as any).session_id)`
          - `type === 'text'`: call `updateLastAgentMessage(event.data as string)`
          - `type === 'tool_call'`: add tool_call message: `addChatMessage({ id: crypto.randomUUID(), role: 'agent', content: '', timestamp: Date.now(), type: 'tool_call', toolName: (event.data as any).name })`
          - `type === 'tool_result'`: check if result contains `proposed_diff` key — if so, extract diff, call `setPendingDiff(diff)`, and update the last streaming agent message to include the diff: find last agent message with `streaming: true`, set `diff: diff` on it
          - `type === 'done'`: mark last streaming message as `streaming: false`, call `setIsAgentStreaming(false)`
      11. On error (try/catch around the for-await loop): add error message `addChatMessage({ id: crypto.randomUUID(), role: 'agent', content: 'Connection lost. Please try again.', timestamp: Date.now(), type: 'error' })`, call `setIsAgentStreaming(false)`
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/12-flow/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/Chat/MessageList.tsx` contains `chatMessages`
    - `frontend/src/components/Chat/MessageList.tsx` contains `No messages yet`
    - `frontend/src/components/Chat/MessageList.tsx` contains `scrollHeight` (auto-scroll)
    - `frontend/src/components/Chat/MessageBubble.tsx` contains `streaming`
    - `frontend/src/components/Chat/MessageBubble.tsx` contains `pre-wrap`
    - `frontend/src/components/Chat/DiffProposal.tsx` contains `Apply Changes`
    - `frontend/src/components/Chat/DiffProposal.tsx` contains `Reject Changes`
    - `frontend/src/components/Chat/DiffProposal.tsx` contains `applyAgentDiff`
    - `frontend/src/components/Chat/DiffProposal.tsx` contains `Proposed Changes`
    - `frontend/src/components/Chat/ChatInput.tsx` contains `streamAgentChat`
    - `frontend/src/components/Chat/ChatInput.tsx` contains `setChatSessionId`
    - `frontend/src/components/Chat/ChatInput.tsx` contains `aria-label="Send message"`
    - `frontend/src/components/Chat/ChatInput.tsx` contains `Shift` (Shift+Enter newline check)
    - `frontend/src/components/Chat/ChatInput.tsx` contains `setIsAgentStreaming(false)` (in done handler and error handler)
    - `frontend/src/components/Chat/ChatInput.tsx` contains `proposed_diff` (tool_result diff detection)
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>All chat UI components render messages, stream agent responses, display diff proposals with Apply/Reject, and handle the full send-receive-render cycle via SSE</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — zero TypeScript errors
- `cd frontend && pnpm dev` — app starts, ChatPanel importable (not yet wired into EditorPage — that's Plan 04)
- All eight component files exist in `frontend/src/components/Chat/`
</verification>

<success_criteria>
- ChatPanel renders as right sidebar with drag-resize, header with mode toggle and close button
- ModeToggle switches modes without clearing chat (per D-07)
- MessageList auto-scrolls, shows empty state per mode
- MessageBubble renders user/agent messages with correct styling and streaming cursor
- DiffProposal shows structured items with Apply/Reject; Apply calls applyAgentDiff
- ChatInput sends via streamAgentChat, captures session ID, renders streaming events
- All components use existing CSS custom properties, no new color values
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-canvas-agent/20-03-SUMMARY.md`
</output>
