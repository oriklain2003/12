---
phase: 15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes
plan: 07
type: execute
wave: 2
depends_on: ["15-06"]
files_modified:
  - backend/tests/test_signal_health_analyzer.py
  - backend/tests/test_integration_pipelines.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "SignalHealthAnalyzerCube tests verify orchestration of both detection layers with mocked signal functions"
    - "Integration pipeline tests verify data flows correctly through multi-cube chains via WorkflowExecutor"
    - "All cube test files across the entire phase run together without conflicts"
  artifacts:
    - path: "backend/tests/test_signal_health_analyzer.py"
      provides: "SignalHealthAnalyzerCube unit tests"
      min_lines: 80
    - path: "backend/tests/test_integration_pipelines.py"
      provides: "Multi-cube integration pipeline tests"
      min_lines: 100
  key_links:
    - from: "backend/tests/test_signal_health_analyzer.py"
      to: "backend/app/cubes/signal_health_analyzer.py"
      via: "patch signal module functions"
      pattern: "patch.*app\\.cubes\\.signal_health_analyzer"
    - from: "backend/tests/test_integration_pipelines.py"
      to: "backend/app/engine/executor.py"
      via: "execute_graph with mocked registry"
      pattern: "execute_graph"
---

<objective>
Create unit tests for SignalHealthAnalyzerCube (orchestrator cube) and integration pipeline tests verifying multi-cube data flow through the WorkflowExecutor.

Purpose: The signal health analyzer orchestrates both detection layers and is the most complex cube. Integration tests verify realistic cube chains work end-to-end with mocked data. This plan depends on Plan 06 (signal module tests) to ensure the underlying modules are tested first.
Output: 2 test files, all passing. Full test suite run across all phase 15 tests.
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-CONTEXT.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-RESEARCH.md
@.planning/STATE.md (for signal_health_analyzer decisions)

@backend/app/cubes/signal_health_analyzer.py
@backend/app/engine/executor.py
@backend/tests/test_executor.py (pattern reference for make_node/make_edge and execute_graph testing)
@backend/tests/test_stream_graph.py (pattern reference for workflow execution)

<interfaces>
<!-- From test_executor.py -- helpers needed for integration tests -->
From backend/tests/test_executor.py:
```python
def make_node(node_id: str, cube_id: str, params: dict | None = None) -> WorkflowNode
def make_edge(edge_id: str, source: str, target: str, source_handle: str = "result", target_handle: str = "value") -> WorkflowEdge
```

From backend/app/schemas/workflow.py:
```python
class WorkflowGraph(BaseModel):
    nodes: list[WorkflowNode]
    edges: list[WorkflowEdge]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SignalHealthAnalyzerCube tests</name>
  <files>backend/tests/test_signal_health_analyzer.py</files>
  <action>
Read full source of `signal_health_analyzer.py` to understand: inputs (hex_list, full_result, target_phase, classify_mode), outputs (flight_ids, count, events, stats_summary), how it orchestrates rule_based and kalman modules.

**test_signal_health_analyzer.py** — mock signal module functions (not the engine directly, since the cube delegates to signal modules):
- test_cube_metadata: cube_id="signal_health_analyzer", category=ANALYSIS
- test_cube_inputs: verify hex_list, full_result, target_phase, classify_mode inputs
- test_cube_outputs: verify flight_ids, count, events, stats_summary outputs
- test_empty_hex_list_guard: empty hex_list -> early return with empty results
- test_full_result_extraction: pass full_result dict, verify hex_list extracted from it (checks both hex_list and flight_ids keys per STATE.md decision)
- test_basic_detection: mock detect_integrity_events_async and classify_flight_async to return sample events, verify cube orchestrates both and combines results
- test_classify_mode_anomaly: classify_mode="anomaly" -> only non-normal events returned
- test_classify_mode_stable: classify_mode="stable" -> hexes with zero non-normal events returned (per STATE.md: "returns hexes with zero non-normal events")
- test_target_phase_filter: target_phase="takeoff" -> only events with entry altitude < 5000ft
- test_stats_summary: verify stats_summary includes event counts by classification

Patch at the import location in signal_health_analyzer.py: `patch("app.cubes.signal_health_analyzer.detect_integrity_events_async")`, `patch("app.cubes.signal_health_analyzer.classify_flight_async")`, etc. Read the import statements at the top of signal_health_analyzer.py to determine exact patch targets.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_signal_health_analyzer.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>SignalHealthAnalyzerCube tests pass covering orchestration, classify_mode filtering, target_phase filtering, and full_result extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Integration pipeline tests</name>
  <files>backend/tests/test_integration_pipelines.py</files>
  <action>
Create integration tests that verify multi-cube pipelines through the WorkflowExecutor. Use the established pattern from test_executor.py: build WorkflowGraph with make_node/make_edge helpers, mock the cube registry, and run execute_graph.

**test_integration_pipelines.py:**

Duplicate `make_node` and `make_edge` helpers at the top of the file (following existing pattern of not importing helpers cross-file).

Test at least 3 pipeline scenarios from RESEARCH.md:

1. **test_fr_analysis_pipeline:** AllFlights -> FilterFlights -> GetAnomalies
   - Mock AllFlights.execute() returning flight_ids
   - Wire __full_result__ from AllFlights to FilterFlights
   - Wire __full_result__ from FilterFlights to GetAnomalies
   - Verify all nodes execute successfully, data flows through

2. **test_alison_filter_chain:** AlisonFlights -> SquawkFilter -> DarkFlightDetector
   - Mock AlisonFlights.execute() returning hex_list
   - Wire hex_list through the chain
   - Verify hex_list propagates correctly

3. **test_signal_analysis_pipeline:** AlisonFlights -> SignalHealthAnalyzer
   - Mock AlisonFlights returning hex_list
   - Wire __full_result__ to signal analyzer
   - Verify signal analysis receives hex_list

For each pipeline:
- Create mock cube classes that return realistic data structures (matching actual cube output schemas)
- Mock the CubeRegistry to return mock cube instances
- Use `patch("app.engine.executor.registry", mock_registry)`
- Call execute_graph(graph) and assert all nodes have status="done"
- Verify output data structure of final node

Do NOT connect to real DB. All cubes are mocked. The point is testing the executor's wiring, not cube logic.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_integration_pipelines.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Integration pipeline tests pass verifying data flow through 3 realistic multi-cube chains via WorkflowExecutor.</done>
</task>

</tasks>

<verification>
# Run all phase 15 tests together to verify no conflicts
cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_echo_cube.py tests/test_add_numbers.py tests/test_count_by_field.py tests/test_geo_temporal_playback.py tests/test_icao24_lookup.py tests/test_all_flights.py tests/test_alison_flights.py tests/test_get_anomalies.py tests/test_get_flight_course.py tests/test_get_learned_paths.py tests/test_squawk_filter.py tests/test_registration_country_filter.py tests/test_area_spatial_filter.py tests/test_signal_rule_based.py tests/test_signal_kalman.py tests/test_signal_health_analyzer.py tests/test_geo_country_loader.py tests/test_geo_fir_loader.py tests/test_geo_land_water_loader.py tests/test_integration_pipelines.py -v --tb=short
</verification>

<success_criteria>
- 2 test files created, all tests pass
- SignalHealthAnalyzerCube tested with mocked signal modules (not live DB)
- Integration tests verify 3 realistic multi-cube pipelines through WorkflowExecutor
- Full test suite (all plans) runs together without conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-07-SUMMARY.md`
</output>
