---
phase: 15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_signal_rule_based.py
  - backend/tests/test_signal_kalman.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Rule-based pure functions (score_event, classify_event) produce correct scores for jamming/spoofing/transponder scenarios"
    - "Rule-based async functions (detect_integrity_events_async, detect_transponder_shutdowns_async) work with mocked DB"
    - "Kalman pure functions (kalman_filter, detect_position_jumps, detect_altitude_divergence) flag anomalies in synthetic data"
    - "Kalman async functions (classify_flight_async) work with mocked DB"
  artifacts:
    - path: "backend/tests/test_signal_rule_based.py"
      provides: "Rule-based signal module tests"
      min_lines: 100
    - path: "backend/tests/test_signal_kalman.py"
      provides: "Kalman signal module tests"
      min_lines: 100
  key_links:
    - from: "backend/tests/test_signal_rule_based.py"
      to: "backend/app/signal/rule_based.py"
      via: "direct import for pure functions, patch for async"
      pattern: "from app\\.signal\\.rule_based import"
    - from: "backend/tests/test_signal_kalman.py"
      to: "backend/app/signal/kalman.py"
      via: "direct import for pure functions, patch for async"
      pattern: "from app\\.signal\\.kalman import"
---

<objective>
Create unit tests for both signal detection modules: rule_based.py (integrity events, transponder shutdowns, scoring) and kalman.py (Kalman filter, position jumps, altitude divergence, physics cross-validation).

Purpose: Signal modules are the most complex logic in the system. Testing pure computation functions directly (no mocking) ensures detection accuracy. Async DB functions are tested with mocked engine. Realistic synthetic data models real-world jamming, spoofing, and transponder gap scenarios.
Output: 2 test files, all passing
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-CONTEXT.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-RESEARCH.md
@.planning/STATE.md (for signal module key decisions)

@backend/app/signal/rule_based.py
@backend/app/signal/kalman.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rule-based signal module tests</name>
  <files>backend/tests/test_signal_rule_based.py</files>
  <action>
Read full source of `rule_based.py` to understand: score_event(), classify_event(), detect_integrity_events_async(), detect_transponder_shutdowns_async(), get_coverage_baseline(). Note all scoring constants and thresholds.

**test_signal_rule_based.py:**

Pure function tests (no mocking):
- test_score_event_jamming: event with nacp_zero=True, nic_zero=True, low mean_seen_pos, good coverage -> high jamming_score (>6), low spoofing_score
- test_score_event_spoofing: event with high alt_divergence, position anomalies -> high spoofing_score
- test_score_event_transponder_off: event with source="transponder_off" -> passthrough without re-scoring
- test_score_event_normal: normal event with all healthy indicators -> low scores
- test_classify_event_jamming: scored event with jamming_score > threshold -> classified as "jamming"
- test_classify_event_spoofing: scored event with spoofing_score > threshold -> classified as "spoofing"
- test_classify_event_anomaly: moderate scores -> classified as "anomaly"
- test_classify_event_normal: low scores -> classified as "normal"

Async function tests (mock engine):
- test_detect_integrity_events_async: mock DB returning position rows with integrity drops, verify event detection and structure
- test_detect_integrity_events_empty: mock empty DB result -> no events
- test_detect_transponder_shutdowns_async: mock DB with last_seen/first_seen gaps, verify shutdown detection
- test_get_coverage_baseline: mock DB returning coverage stats, verify baseline dict structure

Use realistic synthetic data based on actual thresholds from source code. Always use timezone-aware datetimes (datetime(..., tzinfo=timezone.utc)).

Patch at `app.signal.rule_based.engine` for async functions.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_signal_rule_based.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>
  <done>Rule-based signal module tests pass covering scoring, classification, and async detection with realistic synthetic data.</done>
</task>

<task type="auto">
  <name>Task 2: Kalman signal module tests</name>
  <files>backend/tests/test_signal_kalman.py</files>
  <action>
Read full source of `kalman.py` to understand: kalman_filter(), detect_position_jumps(), detect_altitude_divergence(), physics_cross_validation(), haversine_km(), latlon_to_enu(), classify_flight(), classify_flight_async(), fetch_positions_async(), fetch_time_range_async(). Note constants: CHI2_THRESHOLD=13.82, POSITION_JUMP_KM=55.56, ALT_DIVERGENCE_FT=1000.

**test_signal_kalman.py:**

Pure function tests (no mocking):
- test_haversine_km: known distance between two points (e.g., TLV to Jerusalem ~60km)
- test_latlon_to_enu: coordinate conversion produces expected ENU values
- test_kalman_filter_normal: smooth trajectory -> no flagged points
- test_kalman_filter_jump: trajectory with sudden 100km position jump -> flagged point(s)
- test_detect_position_jumps: positions with gap > POSITION_JUMP_KM -> detected
- test_detect_position_jumps_normal: smooth positions -> no jumps
- test_detect_altitude_divergence: barometric vs geometric altitude divergence > ALT_DIVERGENCE_FT -> detected
- test_detect_altitude_divergence_normal: consistent altitudes -> no divergence
- test_physics_cross_validation: impossibly fast movement (>Mach 2) between points -> flagged
- test_physics_cross_validation_normal: reasonable speeds -> no flags
- test_classify_flight: positions + detection results -> flight classification dict

Async function tests (mock engine):
- test_classify_flight_async: mock DB returning position rows for a hex, verify classification result structure
- test_fetch_positions_async: mock DB, verify position list returned
- test_classify_flight_async_empty: no positions for hex -> empty/safe classification

Use numpy for generating synthetic position arrays where needed. Always use timezone-aware datetimes.

Patch at `app.signal.kalman.engine` for async functions.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_signal_kalman.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>
  <done>Kalman signal module tests pass covering all pure computation functions and async DB functions with synthetic flight data.</done>
</task>

</tasks>

<verification>
cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_signal_rule_based.py tests/test_signal_kalman.py -v --tb=short
</verification>

<success_criteria>
- 2 test files created, all tests pass
- Pure functions tested directly with no mocking -- jamming, spoofing, position jump, altitude divergence scenarios
- Async DB functions tested with mocked engine
- Realistic synthetic data based on actual detection thresholds from source code
- All datetimes are timezone-aware (UTC)
</success_criteria>

<output>
After completion, create `.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-06-SUMMARY.md`
</output>
