---
phase: 15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_get_flight_course.py
  - backend/tests/test_get_learned_paths.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "GetFlightCourseCube tests verify points mode and lines mode output with mocked DB"
    - "GetLearnedPathsCube tests verify query with optional filters and GeoJSON coordinate transformation"
  artifacts:
    - path: "backend/tests/test_get_flight_course.py"
      provides: "GetFlightCourseCube unit tests"
      min_lines: 50
    - path: "backend/tests/test_get_learned_paths.py"
      provides: "GetLearnedPathsCube unit tests"
      min_lines: 50
  key_links:
    - from: "backend/tests/test_get_flight_course.py"
      to: "backend/app/cubes/get_flight_course.py"
      via: "patch('app.cubes.get_flight_course.engine')"
      pattern: "patch.*app\\.cubes\\.get_flight_course\\.engine"
---

<objective>
Create unit tests for GetFlightCourseCube and GetLearnedPathsCube -- the remaining v1.0 data-source cubes.

Purpose: These cubes handle GeoJSON transformation and coordinate order conversion. Tests ensure correct geometry output and optional filter handling.
Output: 2 test files, all passing
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-CONTEXT.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-RESEARCH.md

@backend/tests/test_dark_flight_detector.py (pattern reference)
@backend/app/cubes/get_flight_course.py
@backend/app/cubes/get_learned_paths.py
@backend/tests/conftest.py (for make_mock_db_conn if available)
</context>

<tasks>

<task type="auto">
  <name>Task 1: GetFlightCourseCube tests</name>
  <files>backend/tests/test_get_flight_course.py</files>
  <action>
Read full source of `get_flight_course.py` to understand: input params (flight_ids, mode), output structure, points vs lines mode, string split handling for flight_ids.

**test_get_flight_course.py** — patch `app.cubes.get_flight_course.engine`:
- test_cube_metadata: cube_id="get_flight_course", category=DATA_SOURCE
- test_cube_inputs: verify flight_ids input, mode input
- test_cube_outputs: verify expected outputs
- test_points_mode: mock DB with position rows (flight_id, lat, lon, alt, ts), verify GeoJSON point output
- test_lines_mode: mock DB, verify GeoJSON LineString output grouping positions by flight
- test_empty_flight_ids: empty or None input -> early return with empty result
- test_string_flight_ids: flight_ids as comma-separated string -> correctly split and queried

Follow existing DB mocking patterns.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_get_flight_course.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>GetFlightCourseCube tests pass covering both output modes, empty guards, and string splitting.</done>
</task>

<task type="auto">
  <name>Task 2: GetLearnedPathsCube tests</name>
  <files>backend/tests/test_get_learned_paths.py</files>
  <action>
Read full source of `get_learned_paths.py` to understand: input params (origin, destination, polygon), output structure, coordinate order conversion (DB stores lat/lon, GeoJSON outputs lon/lat).

**test_get_learned_paths.py** — patch `app.cubes.get_learned_paths.engine`:
- test_cube_metadata: cube_id="get_learned_paths", category=DATA_SOURCE
- test_cube_inputs: verify origin, destination, polygon inputs
- test_cube_outputs: verify paths, count outputs
- test_basic_query: mock DB with learned path rows, verify result structure and GeoJSON coordinate order
- test_empty_result: no paths returned -> empty list, count=0
- test_origin_filter: pass origin param
- test_polygon_filter: pass polygon param, verify spatial filtering logic
- test_coordinate_order: verify lat/lon from DB is converted to lon/lat in GeoJSON output (critical correctness check)

Follow existing DB mocking patterns.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_get_learned_paths.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>GetLearnedPathsCube tests pass covering query filters, coordinate order, and empty guards.</done>
</task>

</tasks>

<verification>
cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_get_flight_course.py tests/test_get_learned_paths.py -v --tb=short
</verification>

<success_criteria>
- 2 test files created, all tests pass
- Points and lines modes both tested for GetFlightCourseCube
- Coordinate order conversion (lat/lon -> lon/lat) verified for GetLearnedPathsCube
- Empty input guards tested for both cubes
</success_criteria>

<output>
After completion, create `.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-03-SUMMARY.md`
</output>
