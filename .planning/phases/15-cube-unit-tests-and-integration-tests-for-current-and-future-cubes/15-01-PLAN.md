---
phase: 15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_echo_cube.py
  - backend/tests/test_add_numbers.py
  - backend/tests/test_count_by_field.py
  - backend/tests/test_geo_temporal_playback.py
  - backend/tests/test_icao24_lookup.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Pure-logic cubes pass all tests without any DB mocking"
    - "Shared conftest.py provides make_mock_db_conn helper for all subsequent plans"
    - "ICAO24 lookup resolves known hex ranges and region groups correctly"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Shared DB mock helper fixture"
      contains: "make_mock_db_conn"
    - path: "backend/tests/test_echo_cube.py"
      provides: "EchoCube unit tests"
      min_lines: 20
    - path: "backend/tests/test_add_numbers.py"
      provides: "AddNumbersCube unit tests"
      min_lines: 25
    - path: "backend/tests/test_count_by_field.py"
      provides: "CountByFieldCube unit tests"
      min_lines: 40
    - path: "backend/tests/test_geo_temporal_playback.py"
      provides: "GeoTemporalPlaybackCube unit tests"
      min_lines: 25
    - path: "backend/tests/test_icao24_lookup.py"
      provides: "icao24_lookup module tests"
      min_lines: 40
  key_links: []
---

<objective>
Create shared test infrastructure (conftest.py) and unit tests for all pure-logic cubes (echo, add_numbers, count_by_field, geo_temporal_playback) plus the icao24_lookup module.

Purpose: Establish shared fixtures and test the simplest cubes first -- no DB mocking needed for pure-logic cubes, pure function testing for icao24_lookup. This provides the conftest.py that all subsequent plans use.
Output: conftest.py + 5 test files, all passing
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-CONTEXT.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-RESEARCH.md

@backend/tests/test_dark_flight_detector.py (pattern reference)
@backend/tests/test_executor.py (pattern reference for make_node/make_edge)
@backend/app/cubes/echo_cube.py
@backend/app/cubes/add_numbers.py
@backend/app/cubes/count_by_field.py
@backend/app/cubes/geo_temporal_playback.py
@backend/app/cubes/icao24_lookup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conftest.py and pure-logic cube tests</name>
  <files>backend/tests/conftest.py, backend/tests/test_echo_cube.py, backend/tests/test_add_numbers.py, backend/tests/test_count_by_field.py, backend/tests/test_geo_temporal_playback.py</files>
  <action>
Create `backend/tests/conftest.py` with the shared `make_mock_db_conn()` helper function as specified in RESEARCH.md code example. This helper creates a mock async DB connection context manager accepting optional results (None for empty, single MagicMock, or list for side_effect).

Create test files for 4 pure-logic cubes following existing test_dark_flight_detector.py patterns (inline imports inside test functions, metadata tests first):

**test_echo_cube.py:**
- test_cube_metadata: cube_id="echo", category=OUTPUT
- test_cube_inputs/outputs: "value" input, "result" output
- test_echo_returns_input: execute(value="hello") -> {"result": "hello"}
- test_echo_empty_input: execute() -> {"result": ""}

**test_add_numbers.py:**
- test_cube_metadata: cube_id="add_numbers", category=ANALYSIS
- test_add_integers: execute(a=3, b=4) -> {"sum": 7.0}
- test_add_floats: execute(a=1.5, b=2.7) -> sum close to 4.2
- test_add_zero_defaults: execute() -> {"sum": 0.0}
- test_add_negative: execute(a=-5, b=3) -> {"sum": -2.0}

**test_count_by_field.py:**
- test_cube_metadata: cube_id="count_by_field", category=AGGREGATION
- test_count_basic: data=[{type: "A"}, {type: "B"}, {type: "A"}], group_by_field="type" -> counts sorted desc
- test_count_empty_data: data=[] -> empty counts
- test_count_missing_field: data has rows without the group_by_field -> should handle gracefully (NaN/None group)
- test_count_full_result_dict: data is a full_result dict (with nested arrays) -> cube extracts largest array

**test_geo_temporal_playback.py:**
- test_cube_metadata: cube_id="geo_temporal_playback", category=OUTPUT, widget="geo_playback"
- test_passthrough: data flows through unchanged
- test_empty_data: empty input returns empty output

All tests use `async def` test functions (asyncio_mode="auto" handles them).
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_echo_cube.py tests/test_add_numbers.py tests/test_count_by_field.py tests/test_geo_temporal_playback.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All pure-logic cube tests pass. conftest.py exists with make_mock_db_conn helper.</done>
</task>

<task type="auto">
  <name>Task 2: Create icao24_lookup module tests</name>
  <files>backend/tests/test_icao24_lookup.py</files>
  <action>
Create `backend/tests/test_icao24_lookup.py` testing the pure functions in `app/cubes/icao24_lookup.py`. Read the source file first to understand exact function signatures, ICAO24_RANGES data, TAIL_PREFIXES data, and region definitions.

Test cases (all pure function tests, no mocking needed):

**resolve_country_from_hex:**
- test_iran_hex: "730000" -> ("Iran", "black")
- test_us_hex: "A00000" -> ("United States", None or correct region)
- test_unknown_hex: "000000" or out-of-range hex -> None
- test_hex_case_insensitive: verify lowercase hex works

**resolve_country_from_registration:**
- test_known_prefix: e.g., "N12345" -> US registration
- test_unknown_prefix: random string -> None

**expand_regions:**
- test_expand_black: expand_regions(["black"]) contains "Iran", "Syria"; does NOT contain "Israel"
- test_expand_gray: expand_regions(["gray"]) returns gray-list countries
- test_expand_country_names: expand_regions(["France"]) -> ["France"]
- test_expand_mixed: regions + country names combined

Use inline imports inside each test function.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_icao24_lookup.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All icao24_lookup tests pass covering hex resolution, tail prefix lookup, and region expansion.</done>
</task>

</tasks>

<verification>
cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/conftest.py tests/test_echo_cube.py tests/test_add_numbers.py tests/test_count_by_field.py tests/test_geo_temporal_playback.py tests/test_icao24_lookup.py -v --tb=short
</verification>

<success_criteria>
- conftest.py exists with make_mock_db_conn helper
- 5 test files created and all tests pass
- Pure-logic cubes tested without any DB mocking
- icao24_lookup pure functions tested for hex resolution, tail prefixes, and region expansion
</success_criteria>

<output>
After completion, create `.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-01-SUMMARY.md`
</output>
