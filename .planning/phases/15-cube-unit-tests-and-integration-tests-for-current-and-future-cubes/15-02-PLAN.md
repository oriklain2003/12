---
phase: 15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_all_flights.py
  - backend/tests/test_alison_flights.py
  - backend/tests/test_get_anomalies.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "AllFlightsCube tests verify query behavior with mocked DB for time range, callsign, polygon filters"
    - "AlisonFlightsCube tests verify both fast path (no join) and slow path (positions join) with mocked DB"
    - "GetAnomaliesCube tests verify filtering by flight_ids, severity, and anomaly status"
  artifacts:
    - path: "backend/tests/test_all_flights.py"
      provides: "AllFlightsCube unit tests"
      min_lines: 60
    - path: "backend/tests/test_alison_flights.py"
      provides: "AlisonFlightsCube unit tests"
      min_lines: 60
    - path: "backend/tests/test_get_anomalies.py"
      provides: "GetAnomaliesCube unit tests"
      min_lines: 50
  key_links:
    - from: "backend/tests/test_all_flights.py"
      to: "backend/app/cubes/all_flights.py"
      via: "patch('app.cubes.all_flights.engine')"
      pattern: "patch.*app\\.cubes\\.all_flights\\.engine"
    - from: "backend/tests/test_alison_flights.py"
      to: "backend/app/cubes/alison_flights.py"
      via: "patch('app.cubes.alison_flights.engine')"
      pattern: "patch.*app\\.cubes\\.alison_flights\\.engine"
---

<objective>
Create unit tests for the three v1.0 data-source cubes that query the database: AllFlightsCube, AlisonFlightsCube, and GetAnomaliesCube.

Purpose: These are the foundational data-source cubes used in most workflows. Testing them with mocked DB ensures query logic and result transformation are correct.
Output: 3 test files, all passing
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-CONTEXT.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-RESEARCH.md

@backend/tests/test_dark_flight_detector.py (pattern reference)
@backend/tests/test_filter_flights.py (pattern reference for DB mocking)
@backend/app/cubes/all_flights.py
@backend/app/cubes/alison_flights.py
@backend/app/cubes/get_anomalies.py
@backend/tests/conftest.py (for make_mock_db_conn if created by plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: AllFlightsCube and AlisonFlightsCube tests</name>
  <files>backend/tests/test_all_flights.py, backend/tests/test_alison_flights.py</files>
  <action>
Read the full source of `all_flights.py` and `alison_flights.py` to understand all parameters, query paths, and result transformation.

**test_all_flights.py** — patch `app.cubes.all_flights.engine`:
- test_cube_metadata: cube_id="all_flights", category=DATA_SOURCE
- test_cube_inputs: verify expected inputs (time_range_seconds, callsign, polygon, etc.)
- test_cube_outputs: verify expected outputs (flights, flight_ids, count)
- test_basic_query: mock DB returning 2 flight rows, verify result dict structure
- test_empty_result: mock DB returning no rows, verify empty flights list and count=0
- test_polygon_filter: test with polygon parameter triggering the position-based filtering path (may need side_effect for multiple DB calls)
- test_absolute_time_range: if cube supports absolute time params, test that path

**test_alison_flights.py** — patch `app.cubes.alison_flights.engine`:
- test_cube_metadata: cube_id="alison_flights", category=DATA_SOURCE
- test_cube_inputs/outputs: verify hex_list output, time_range input
- test_fast_path: no callsign/altitude/polygon -> single query on aircraft table only
- test_slow_path_callsign: callsign filter triggers positions join (side_effect for 2 queries)
- test_empty_result: verify empty hex_list and count=0
- test_polygon_filter: polygon parameter adds spatial filtering

Use `make_mock_db_conn()` from conftest if available, otherwise build inline mock following test_filter_flights.py patterns. Use mock_result.keys().return_value for column names matching what each cube expects.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_all_flights.py tests/test_alison_flights.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>
  <done>AllFlightsCube and AlisonFlightsCube tests pass covering metadata, empty guards, basic queries, and filter paths.</done>
</task>

<task type="auto">
  <name>Task 2: GetAnomaliesCube tests</name>
  <files>backend/tests/test_get_anomalies.py</files>
  <action>
Read the full source of `get_anomalies.py` to understand parameters (flight_ids, min_severity, is_anomaly) and result structure.

**test_get_anomalies.py** — patch `app.cubes.get_anomalies.engine`:
- test_cube_metadata: cube_id="get_anomalies", category=DATA_SOURCE
- test_cube_inputs: verify flight_ids, min_severity, is_anomaly inputs
- test_cube_outputs: verify anomalies, count outputs
- test_basic_query: mock DB returning anomaly rows, verify result dict
- test_empty_result: no anomalies returned -> empty list, count=0
- test_flight_ids_filter: pass flight_ids, verify mock was called (don't assert SQL content)
- test_severity_filter: pass min_severity param
- test_empty_flight_ids_guard: empty or None flight_ids -> early return with empty results

Follow the same DB mocking pattern as Task 1.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_get_anomalies.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>GetAnomaliesCube tests pass covering metadata, empty guards, basic query, and filter parameters.</done>
</task>

</tasks>

<verification>
cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_all_flights.py tests/test_alison_flights.py tests/test_get_anomalies.py -v --tb=short
</verification>

<success_criteria>
- 3 test files created, all tests pass
- DB mocking uses patch at import location (app.cubes.xxx.engine)
- Both fast/slow query paths tested for AlisonFlightsCube
- Empty input guards tested for all three cubes
</success_criteria>

<output>
After completion, create `.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-02-SUMMARY.md`
</output>
