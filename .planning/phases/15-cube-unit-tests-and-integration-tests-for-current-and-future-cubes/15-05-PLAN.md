---
phase: 15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_geo_country_loader.py
  - backend/tests/test_geo_fir_loader.py
  - backend/tests/test_geo_land_water_loader.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Country loader correctly classifies points over known countries and returns None for ocean"
    - "FIR loader correctly classifies points within known FIR boundaries"
    - "Land/water loader correctly distinguishes land from water using STRtree spatial index"
  artifacts:
    - path: "backend/tests/test_geo_country_loader.py"
      provides: "Country loader unit tests"
      min_lines: 40
    - path: "backend/tests/test_geo_fir_loader.py"
      provides: "FIR loader unit tests"
      min_lines: 40
    - path: "backend/tests/test_geo_land_water_loader.py"
      provides: "Land/water loader unit tests"
      min_lines: 40
  key_links:
    - from: "backend/tests/test_geo_country_loader.py"
      to: "backend/app/geo/country_loader.py"
      via: "direct import"
      pattern: "from app\\.geo\\.country_loader import"
---

<objective>
Create unit tests for the three geo loader modules: country_loader, fir_loader, and land_water_loader.

Purpose: These modules load bundled GeoJSON data and provide spatial classification functions. Tests verify correct point classification against real geographic boundaries. No DB mocking needed -- these are pure spatial functions operating on bundled static data.
Output: 3 test files, all passing
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-CONTEXT.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-RESEARCH.md

@backend/app/geo/country_loader.py
@backend/app/geo/fir_loader.py
@backend/app/geo/land_water_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Country loader and FIR loader tests</name>
  <files>backend/tests/test_geo_country_loader.py, backend/tests/test_geo_fir_loader.py</files>
  <action>
Read full source of `country_loader.py` and `fir_loader.py` to understand exported functions and data structures.

**test_geo_country_loader.py** — pure function tests (no mocking, uses bundled GeoJSON):
- test_classify_point_known_country: Tel Aviv (32.08, 34.78) -> country="Israel" (or matching property name from countries.geojson)
- test_classify_point_ocean: Atlantic Ocean (30.0, -40.0) -> None
- test_classify_point_another_country: Paris (48.85, 2.35) -> "France"
- test_list_countries_count: list_countries() returns >100 entries
- test_get_country_polygon: known country returns a Shapely polygon (not None)
- test_get_country_polygon_unknown: unknown country returns None

NOTE: Property keys in countries.geojson are `name` and `ISO3166-1-Alpha-3` (per STATE.md). Verify actual property names when reading source.

**test_geo_fir_loader.py** — pure function tests:
- test_classify_point_known_fir: point in known European FIR (e.g., London FIR ~51.5, -0.1)
- test_classify_point_outside_coverage: point far from European FIR coverage -> None
- test_list_firs_count: list_firs() returns entries (FIR dataset is European)
- test_get_fir_polygon: known FIR returns polygon

Read source to find exact function names and return types before writing tests. FIR property keys are `AV_AIRSPAC` and `AV_NAME`.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_geo_country_loader.py tests/test_geo_fir_loader.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Country loader and FIR loader tests pass, correctly classifying known geographic points.</done>
</task>

<task type="auto">
  <name>Task 2: Land/water loader tests</name>
  <files>backend/tests/test_geo_land_water_loader.py</files>
  <action>
Read full source of `land_water_loader.py` to understand exported functions (is_land, classify_point) and the STRtree spatial index.

**test_geo_land_water_loader.py** — pure function tests:
- test_is_land_over_land: known land point (e.g., Paris 48.85, 2.35) -> True
- test_is_land_over_ocean: mid-Atlantic (30.0, -40.0) -> False
- test_is_land_coastal: point clearly inland vs clearly offshore
- test_classify_point_land: verify classify_point returns "land" or similar
- test_classify_point_water: verify classify_point returns "water" or similar

These tests may take slightly longer to run (STRtree with 1420 polygons) but should still be <5s per test. No mocking needed.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_geo_land_water_loader.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>Land/water loader tests pass, correctly distinguishing land from water points.</done>
</task>

</tasks>

<verification>
cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_geo_country_loader.py tests/test_geo_fir_loader.py tests/test_geo_land_water_loader.py -v --tb=short
</verification>

<success_criteria>
- 3 test files created, all tests pass
- No DB mocking needed -- pure spatial function testing on bundled GeoJSON data
- Known geographic points correctly classified
- Edge cases (ocean, unknown) return None/False as expected
</success_criteria>

<output>
After completion, create `.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-05-SUMMARY.md`
</output>
