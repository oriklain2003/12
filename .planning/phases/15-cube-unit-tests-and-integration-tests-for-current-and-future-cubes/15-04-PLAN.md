---
phase: 15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_squawk_filter.py
  - backend/tests/test_registration_country_filter.py
  - backend/tests/test_area_spatial_filter.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "SquawkFilterCube tests verify dual-provider (FR + Alison) query paths and emergency detection"
    - "RegistrationCountryFilterCube tests verify include/exclude modes and ICAO24 hex resolution"
    - "AreaSpatialFilterCube tests verify polygon containment and movement classification"
  artifacts:
    - path: "backend/tests/test_squawk_filter.py"
      provides: "SquawkFilterCube unit tests"
      min_lines: 80
    - path: "backend/tests/test_registration_country_filter.py"
      provides: "RegistrationCountryFilterCube unit tests"
      min_lines: 80
    - path: "backend/tests/test_area_spatial_filter.py"
      provides: "AreaSpatialFilterCube unit tests"
      min_lines: 80
  key_links:
    - from: "backend/tests/test_squawk_filter.py"
      to: "backend/app/cubes/squawk_filter.py"
      via: "patch('app.cubes.squawk_filter.engine')"
      pattern: "patch.*app\\.cubes\\.squawk_filter\\.engine"
    - from: "backend/tests/test_registration_country_filter.py"
      to: "backend/app/cubes/registration_country_filter.py"
      via: "patch('app.cubes.registration_country_filter.engine')"
      pattern: "patch.*app\\.cubes\\.registration_country_filter\\.engine"
---

<objective>
Create unit tests for the three v2.0 filter cubes: SquawkFilterCube, RegistrationCountryFilterCube, and AreaSpatialFilterCube.

Purpose: These are the most complex filter cubes with dual-provider support, hex resolution, and spatial logic. Thorough testing ensures correctness of filter modes, provider-specific query paths, and edge cases.
Output: 3 test files, all passing
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-CONTEXT.md
@.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-RESEARCH.md

@backend/tests/test_dark_flight_detector.py (pattern reference)
@backend/tests/test_filter_flights.py (pattern reference for complex filter testing)
@backend/app/cubes/squawk_filter.py
@backend/app/cubes/registration_country_filter.py
@backend/app/cubes/area_spatial_filter.py
@backend/app/cubes/icao24_lookup.py (dependency of registration_country_filter)
@backend/tests/conftest.py (for make_mock_db_conn if available)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SquawkFilterCube tests</name>
  <files>backend/tests/test_squawk_filter.py</files>
  <action>
Read full source of `squawk_filter.py` to understand: dual-provider logic (FR vs Alison), custom vs emergency filter modes, code-change detection, input/output params.

**test_squawk_filter.py** — patch `app.cubes.squawk_filter.engine`:
- test_cube_metadata: cube_id="squawk_filter", category=FILTER
- test_cube_inputs: verify flight_ids, hex_list, squawk_codes, filter_mode, full_result inputs
- test_cube_outputs: verify filtered outputs
- test_empty_input_guard: empty flight_ids AND empty hex_list -> early return
- test_custom_mode_fr_provider: pass flight_ids (FR provider), mock DB with squawk data, verify filtering by custom codes
- test_custom_mode_alison_provider: pass hex_list (Alison provider), mock DB with positions having squawk, verify filtering
- test_emergency_mode_fr: emergency mode with FR provider -> filters by 7500/7600/7700
- test_emergency_mode_alison: emergency mode with Alison -> uses positions.emergency column
- test_code_change_detection: mock position data showing squawk transitions, verify code_change_events in full_result
- test_full_result_extraction: pass full_result dict, verify hex_list/flight_ids extraction

Use side_effect for cubes that make multiple DB calls.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_squawk_filter.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>SquawkFilterCube tests pass covering dual-provider, both filter modes, code-change detection, and empty guards.</done>
</task>

<task type="auto">
  <name>Task 2: RegistrationCountryFilterCube and AreaSpatialFilterCube tests</name>
  <files>backend/tests/test_registration_country_filter.py, backend/tests/test_area_spatial_filter.py</files>
  <action>
Read full source of both cubes.

**test_registration_country_filter.py** — patch `app.cubes.registration_country_filter.engine`:
- test_cube_metadata: cube_id="registration_country_filter", category=FILTER
- test_cube_inputs: verify hex_list, countries, regions, filter_mode inputs
- test_empty_hex_list_guard: empty hex_list -> early return
- test_include_mode: pass hex_list with known ICAO24 ranges (from icao24_lookup), countries=["Iran"], filter_mode="include" -> only Iranian hexes returned
- test_exclude_mode: same setup but filter_mode="exclude" -> Iranian hexes removed
- test_region_expansion: regions=["black"] expands to all black-list countries
- test_unknown_hex_handling: hex not in any ICAO24 range -> excluded in include mode, kept in exclude mode (per STATE.md decision)
- test_tail_prefix_fallback: mock DB returning aircraft tail numbers, verify secondary resolution via TAIL_PREFIXES
- test_empty_countries_passthrough: empty countries+regions -> all hexes pass through with warning

**test_area_spatial_filter.py** — patch `app.cubes.area_spatial_filter.engine`:
- test_cube_metadata: cube_id="area_spatial_filter", category=FILTER
- test_cube_inputs: verify flight_ids, hex_list, polygon inputs
- test_empty_input_guard: no flight_ids or hex_list -> early return
- test_fr_provider_polygon: pass flight_ids + polygon (lat/lon pairs), mock DB with position rows inside/outside polygon, verify filtering
- test_alison_provider_polygon: pass hex_list + polygon, mock Alison positions
- test_movement_classification_landing: positions show descent into polygon area -> classify as "landing"
- test_movement_classification_takeoff: positions show ascent from polygon -> classify as "takeoff"
- test_no_polygon_guard: missing polygon -> early return or error

IMPORTANT: Polygon input uses [lat, lon] order (aviation convention). The cube swaps to [lon, lat] internally for Shapely.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_registration_country_filter.py tests/test_area_spatial_filter.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>
  <done>RegistrationCountryFilterCube and AreaSpatialFilterCube tests pass covering filter modes, spatial logic, movement classification, and edge cases.</done>
</task>

</tasks>

<verification>
cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_squawk_filter.py tests/test_registration_country_filter.py tests/test_area_spatial_filter.py -v --tb=short
</verification>

<success_criteria>
- 3 test files created, all tests pass
- Dual-provider paths tested for squawk and area spatial filters
- Include/exclude modes tested for registration country filter
- Movement classification (landing/takeoff/cruise) tested for area spatial filter
- Empty input guards tested for all three cubes
</success_criteria>

<output>
After completion, create `.planning/phases/15-cube-unit-tests-and-integration-tests-for-current-and-future-cubes/15-04-SUMMARY.md`
</output>
