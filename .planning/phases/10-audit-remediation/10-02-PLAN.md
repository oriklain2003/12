---
phase: 10-audit-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Results/ResultsTable.tsx
  - backend/app/engine/executor.py
  - backend/app/routers/workflows.py
autonomous: true
requirements: [BACK-08, BACK-11, GEO-02]

must_haves:
  truths:
    - "ResultsTable truncation warning says 10,000 rows (not 100)"
    - "executor.py apply_row_limit docstring says 10,000 (not 100)"
    - "No POST /api/workflows/{id}/run endpoint exists in workflows.py"
    - "No unused execute_graph import remains in workflows.py"
  artifacts:
    - path: "frontend/src/components/Results/ResultsTable.tsx"
      provides: "Correct truncation warning text"
      contains: "10,000 rows"
    - path: "backend/app/engine/executor.py"
      provides: "Correct docstring limit reference"
      contains: "10,000"
    - path: "backend/app/routers/workflows.py"
      provides: "Clean router without dead endpoint"
  key_links: []
---

<objective>
Fix stale row limit references and remove dead synchronous run endpoint.

Purpose: ResultsTable.tsx and executor.py reference the old 100-row limit (actual is 10,000). The POST /api/workflows/{id}/run endpoint is dead code — frontend uses only the SSE stream path. These are tech debt items from the v1.0 audit.
Output: Three files with corrected content and dead code removed.
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-audit-remediation/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale row limit references in frontend and backend</name>
  <files>
    frontend/src/components/Results/ResultsTable.tsx
    backend/app/engine/executor.py
  </files>
  <action>
    Two surgical text edits:

    1. **ResultsTable.tsx**: Read the file, find the truncation warning string containing "100 rows" (around line 70). Change "Showing first 100 rows" to "Showing first 10,000 rows". Use comma separator for human readability.

    2. **executor.py**: Read the file, find the `apply_row_limit` docstring containing "(100)" (around line 99). Change `defaults to settings.result_row_limit (100)` to `defaults to settings.result_row_limit (10,000)`. Use comma separator for consistency.

    Do NOT change any logic, imports, or other content in either file — only the display/doc strings.
  </action>
  <verify>
    <automated>grep "10,000" frontend/src/components/Results/ResultsTable.tsx backend/app/engine/executor.py</automated>
  </verify>
  <done>Both files reference 10,000 instead of 100 in their respective display string and docstring</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead POST /api/workflows/{id}/run endpoint</name>
  <files>backend/app/routers/workflows.py</files>
  <action>
    Pre-checks before editing (run these greps first):
    1. Grep `frontend/src/` for references to `/run` that do NOT contain `/run/stream` — confirm no frontend code calls the sync endpoint
    2. Grep `backend/tests/` for references to `/run` that do NOT contain `/run/stream` — if tests exist for the dead endpoint, remove those test functions too

    Then edit workflows.py:
    1. Read the full file
    2. Delete the entire `run_workflow` function (the `@router.post("/{workflow_id}/run")` handler and its full body, approximately lines 150-174)
    3. After deletion, check if `execute_graph` is still referenced anywhere else in workflows.py. If not, remove `execute_graph` from the import line (line ~14, importing from `app.engine.executor`)
    4. Also check if any other imports become unused after the deletion (e.g., `select`, `Workflow`, `WorkflowGraph` may still be used by other endpoints — do NOT remove those)

    IMPORTANT: Do NOT remove `stream_graph` or the SSE endpoint — only the synchronous `run_workflow` function.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run python -c "from app.routers.workflows import router; print('import ok')" && ! grep -q "async def run_workflow" app/routers/workflows.py && echo "dead endpoint removed"</automated>
  </verify>
  <done>The POST /api/workflows/{id}/run endpoint no longer exists in workflows.py. The file imports cleanly with no unused imports. The SSE stream endpoint remains intact.</done>
</task>

</tasks>

<verification>
1. grep "10,000" confirms both ResultsTable.tsx and executor.py updated
2. grep "run_workflow" workflows.py returns no matches
3. Python import of workflows router succeeds (no import errors from removed code)
4. Backend tests still pass: `cd backend && uv run pytest`
</verification>

<success_criteria>
1. ResultsTable truncation warning displays "10,000 rows" not "100 rows"
2. executor.py docstring references 10,000 not 100
3. No run_workflow function exists in workflows.py
4. No unused imports remain in workflows.py
5. Backend still starts and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-remediation/10-02-SUMMARY.md`
</output>
