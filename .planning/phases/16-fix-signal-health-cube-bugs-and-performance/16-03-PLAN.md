---
phase: 16-fix-signal-health-cube-bugs-and-performance
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - backend/tests/test_signal_health_analyzer.py
  - backend/tests/test_signal_rule_based.py
  - backend/tests/test_signal_kalman.py
autonomous: true
requirements: [SH-TESTS]

must_haves:
  truths:
    - "All signal test files pass with updated mock targets"
    - "test_signal_health_analyzer.py mocks batch functions instead of per-hex functions"
    - "test_signal_rule_based.py tests get_coverage_baseline without TTL logic"
    - "test_signal_kalman.py tests classify_flight_async with required timestamps"
    - "Full test suite passes (all 236+ tests)"
  artifacts:
    - path: "backend/tests/test_signal_health_analyzer.py"
      provides: "Updated tests for batch-based SignalHealthAnalyzerCube"
    - path: "backend/tests/test_signal_rule_based.py"
      provides: "Updated tests for simplified baseline + batch functions"
    - path: "backend/tests/test_signal_kalman.py"
      provides: "Updated tests for required timestamps + batch positions"
  key_links:
    - from: "backend/tests/test_signal_health_analyzer.py"
      to: "backend/app/cubes/signal_health_analyzer.py"
      via: "mock patch targets match new batch function names"
      pattern: "detect_integrity_events_batch_async"
---

<objective>
Update all signal-related test files to match the new batch function signatures and simplified baseline from Plans 01 and 02. Ensure the full test suite passes.

Purpose: Tests must reflect the new batch architecture — mock targets, return value shapes, and function signatures all changed.

Output: Updated test files with all tests passing.
</objective>

<execution_context>
@/Users/oriklain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oriklain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fix-signal-health-cube-bugs-and-performance/16-CONTEXT.md
@.planning/phases/16-fix-signal-health-cube-bugs-and-performance/16-01-SUMMARY.md
@.planning/phases/16-fix-signal-health-cube-bugs-and-performance/16-02-SUMMARY.md

<interfaces>
<!-- Current test files — executor reads these directly -->
@backend/tests/test_signal_health_analyzer.py
@backend/tests/test_signal_rule_based.py
@backend/tests/test_signal_kalman.py

<!-- New function signatures from Plans 01+02 -->

signal_health_analyzer.py now imports:
- detect_integrity_events_batch_async (was detect_integrity_events_async)
- detect_shutdowns_batch_async (was detect_transponder_shutdowns_async)
- fetch_positions_batch_async (was fetch_time_range_async)
- classify_flight_async (unchanged name but required timestamps, optional positions)
- get_coverage_baseline, score_event, classify_event (unchanged)

Batch functions return dict[str, list[dict]] keyed by hex (not list[dict]).
No _analyze_hex method exists — tests that patch it must be rewritten.

classify_flight_async(hex_code, start_ts, end_ts, positions=None) — no Optional timestamps.
get_coverage_baseline() — no lookback_days param, no TTL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test_signal_health_analyzer.py for batch architecture</name>
  <files>backend/tests/test_signal_health_analyzer.py</files>
  <action>
Update `backend/tests/test_signal_health_analyzer.py` to match the batch-based execute():

**Key changes needed:**

1. **Tests that patched `_analyze_hex`** (test_full_result_extraction_hex_list, test_full_result_extraction_flight_ids, test_classify_mode_anomaly, test_classify_mode_stable, test_stats_summary):
   - Replace `patch.object(cube, "_analyze_hex", ...)` with patches on the batch functions at their import location in `app.cubes.signal_health_analyzer`.
   - Batch mocks must return `dict[str, list[dict]]` keyed by hex, not `list[dict]`.
   - Also mock `fetch_positions_batch_async` returning `dict[str, list[dict]]` (can be empty dicts for tests that don't test Kalman).
   - Mock `classify_flight_async` per-hex returning `{"classification": "normal"}`.

2. **Tests that patched per-hex detection functions** (test_basic_detection, test_kalman_non_normal_event, test_target_phase_takeoff_filters_high_altitude):
   - Change patch targets:
     - `detect_integrity_events_async` -> `detect_integrity_events_batch_async`
     - `detect_transponder_shutdowns_async` -> `detect_shutdowns_batch_async`
     - `fetch_time_range_async` -> remove (no longer imported by signal_health_analyzer)
   - Add `fetch_positions_batch_async` mock.
   - Change return values from `list[dict]` to `dict[str, list[dict]]`. E.g., `AsyncMock(return_value=[event])` becomes `AsyncMock(return_value={"abc123": [event]})`.

3. **Mock `classify_flight_async`** must now receive `positions` kwarg — ensure mocks accept it. The mock should accept `(hex_code, start_ts, end_ts, positions=None)`.

4. **Remove `fetch_time_range_async` patches** — this function is no longer imported by signal_health_analyzer.py.

5. **Test for n_severe_alt_div**: In `test_kalman_non_normal_event`, add assertion `assert ev["n_severe_alt_div"] == 0` (or appropriate value based on mock data) to verify the fix.

6. **All metadata tests** (test_cube_metadata, test_cube_inputs, test_cube_outputs) should remain unchanged.

7. **Ensure `datetime` imports** are present for any timestamp construction needed in mocks.

The test structure and assertions should stay as close to the original as possible — only the mock setup changes.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_signal_health_analyzer.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
- All tests in test_signal_health_analyzer.py pass
- No references to _analyze_hex, detect_integrity_events_async, detect_transponder_shutdowns_async, or fetch_time_range_async
- Batch function mocks return dict[str, list[dict]]
- n_severe_alt_div assertion present
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test_signal_rule_based.py and test_signal_kalman.py</name>
  <files>backend/tests/test_signal_rule_based.py, backend/tests/test_signal_kalman.py</files>
  <action>
**test_signal_rule_based.py changes:**

1. If any tests reference `_BASELINE_TTL`, `_baseline_ts`, or `_baseline_building`, update them to reflect the simplified baseline (no TTL, no building flag).
2. If tests for `get_coverage_baseline()` pass `lookback_days` parameter, remove it (function no longer accepts it).
3. Add a test for `start_coverage_baseline_build()` if not present — mock `build_coverage_baseline_async` and verify it sets `_baseline_cache`.
4. Keep all `score_event` and `classify_event` tests unchanged (these functions did not change).
5. Keep `detect_integrity_events_async` and `detect_transponder_shutdowns_async` tests unchanged (legacy functions still exist).
6. Optionally add tests for batch functions if time allows, but the primary goal is ensuring existing tests pass.

**test_signal_kalman.py changes:**

1. If any tests call `classify_flight_async` with `start_ts=None` or `end_ts=None`, update to pass actual datetime values. The function no longer accepts None.
2. Verify that tests for `classify_flight_async` don't mock `fetch_time_range_async` — if they do, update to pass timestamps directly instead.
3. If tests verify `_serialize_datetimes` behavior (e.g., checking that result contains ISO strings), update expectations. The `start` and `end` fields are still ISO strings (serialized inline), but nested datetime objects in `kalman_results`, `jumps`, `alt_divergence` are now Python objects (not strings).
4. Keep all pure computation function tests unchanged (kalman_filter, detect_position_jumps, detect_altitude_divergence, physics_cross_validation, classify_flight, haversine_km, latlon_to_enu).

After updating both files, run the full test suite to verify nothing is broken.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest tests/test_signal_rule_based.py tests/test_signal_kalman.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
- All tests in test_signal_rule_based.py pass
- All tests in test_signal_kalman.py pass
- No references to removed _serialize_datetimes behavior
- classify_flight_async tests use required timestamps
- get_coverage_baseline tests reflect no-TTL behavior
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification</name>
  <files>backend/tests/</files>
  <action>
Run the full backend test suite to verify no regressions across all 236+ tests. If any tests fail outside the signal test files, investigate and fix — the batch changes should not affect other cube tests, but import-level side effects are possible (e.g., if conftest.py or other test files import signal module functions).

```bash
cd backend && uv run pytest -x -v
```

If failures exist:
1. Check if they are import errors from changed function names
2. Check if integration pipeline tests (test_integration_*.py) reference old function names
3. Fix any issues found

This is a verification-only task — the expected outcome is all tests pass with no changes needed beyond what Tasks 1 and 2 already did.
  </action>
  <verify>
    <automated>cd /Users/oriklain/work/five/tracer/git/12-flow/backend && uv run pytest --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
- Full test suite passes (236+ tests, 0 failures)
- No regressions in non-signal test files
  </done>
</task>

</tasks>

<verification>
```bash
cd backend && uv run pytest --tb=short -q
```
Expected: all tests pass, 0 failures.
</verification>

<success_criteria>
- All signal test files updated with correct mock targets
- Full test suite passes (236+ tests)
- No references to removed functions in test mock targets
</success_criteria>

<output>
After completion, create `.planning/phases/16-fix-signal-health-cube-bugs-and-performance/16-03-SUMMARY.md`
</output>
